/// <reference types="react" />
import { AppQueue } from '../../@types/app';
interface QueueProps {
    queue: AppQueue;
}
export declare const Queue: ({ queue }: QueueProps) => JSX.Element;
export {};
