/// <reference types="react" />
import { AppQueue } from '../../@types/app';
interface QueueNavProps {
    queue: AppQueue;
}
export declare const QueueNav: ({ queue }: QueueNavProps) => JSX.Element;
export {};
